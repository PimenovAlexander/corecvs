/**
 * \file calculationStats.cpp
 * \brief Add Comment Here
 *
 * \date May 10, 2010
 * \author alexander
 */

#include "utils/global.h"

#include "stats/calculationStats.h"

namespace corecvs {

/*
void ViMouseStatistics::printStats()
{
    printf("=============================================================\n");
    printf("Frame height            : %7d px                             \n", hSize);
    printf("Frame width             : %7d px                             \n", wSize);
    for (int i = 0; i < MAX_ID; i++)
    {
        printf("%-20s : %7" PRIu64 " us : %7" PRIu64 " ms  \n", names[i], times[i], times[i] / 1000);
    }
    printf("=============================================================\n");
    printf("Density flow            :        %d  :  %.2lf%%              \n",   flowDensity, ((double)flowDensity   * 100.0 / (hSize * wSize)));
    printf("Density stereo          :        %d  :  %.2lf%%              \n", stereoDensity, ((double)stereoDensity * 100.0 / (hSize * wSize)));
    printf("=============================================================\n");
}
*/

} //namespace corecvs

