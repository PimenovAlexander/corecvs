#include "g12PinPresentation.h"

//void G12PinPresentation::draw(DiagramScene *scene, DiagramItem *blockItem, int x, int y)
//{
//    if (scene == NULL || blockItem == NULL)  return;

//    cout << "G12PinPresentation::draw" << endl;
//    setBrush(scene->itemColor());
//    setPos(x, y);

//    //textItem = new DiagramTextItem(this, scene);
//}
